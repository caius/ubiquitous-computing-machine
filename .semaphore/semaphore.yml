version: "v1.0"
name: "Run things"
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "First block"
    task:
      jobs:
      - name: "job 1"
        commands:
          - "true"

      - name: "job 2"
        commands:
          - "true"

      - name: "job 3"
        commands:
          - "true"

      - name: "job 4"
        commands:
          - "true"

      - name: "job 5"
        commands:
          - "true"

      - name: "job 6"
        commands:
          - "true"

      - name: "job 7"
        commands:
          - "true"

      - name: "job 8"
        commands:
          - "true"

      - name: "job 9"
        commands:
          - "true"

      - name: "job 10"
        commands:
          - "true"

  - name: "Second block"
    task:
      jobs:
      - name: "job 11"
        commands:
          - "true"

      - name: "job 12"
        commands:
          - "true"

      - name: "job 13"
        commands:
          - "true"

      - name: "job 14"
        commands:
          - "true"

      - name: "job 15"
        commands:
          - "true"

      - name: "job 16"
        commands:
          - "true"

      - name: "job 17"
        commands:
          - "true"

      - name: "job 18"
        commands:
          - "true"

      - name: "job 19"
        commands:
          - "true"

      - name: "job 110"
        commands:
          - "true"

  - name: "Third block"
    task:
      jobs:
        - name: "Finisher"
          commands:
            - "true"
